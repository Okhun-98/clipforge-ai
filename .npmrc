# @builder.io/vite-plugin-jsx-loc declares vite@^4||^5 as a peer dep, but the
# project uses vite 7 (which works in practice). pnpm accepted this; npm needs
# legacy-peer-deps to skip the strict check.
legacy-peer-deps=true